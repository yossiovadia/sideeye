"""Side-Eye judge module. Reused unchanged across Phase B (pair grading) and
Phase C (session grading) — the judge does not care whether `produced` is a
single answer or a whole session transcript."""
